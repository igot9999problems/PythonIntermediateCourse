import logging
import logging.config

# Using a logging.conf file to configure the logger

logging.config.fileConfig("logging.conf")

logger = logging.getLogger("simpleExample")
logger.debug("This is a debug message")